// The Age component receives an age prop and renders the
// "Your age is " message followed by the age value, inside a p tag.
function Age({ age }) {
  return (
    <p>
      Your age is {age}
    </p>
  );
}

export default Age;
